[package]
name = "kapitalist_backend"
version = "0.1.0"
authors = ["Florian Wilkens <gh@1wilkens.org>"]
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/1wilkens/kapitalist"

[lib]
name = "kapitalist"
path = "src/lib.rs"

[[bin]]
name = "kapitalist"
path = "src/bin/main.rs"

[dependencies]
log = "0.4"
simplelog = "0.5"

dotenv = "0.13"

serde = "1.0"
serde_derive = "1.0"

chrono = { version = "0.4", features = ["serde"] }

diesel = { version = "1.3", features = ["chrono", "postgres"] }

actix = "0.7"
actix-web = "0.7"
futures = "0.1"

jsonwebtoken = "5.0"

libreauth = "0.10"
