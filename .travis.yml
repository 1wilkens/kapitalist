branches:
  only:
    - develop
language: rust
cache: cargo
rust:
  - nightly-2018-05-26

sudo: false
addons:
  apt:
    packages:
      - libpq-dev

script:
  - cargo build
  - cargo test

notifications:
  email:
    on_success: never
